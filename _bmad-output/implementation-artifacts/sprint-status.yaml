# generated: 2026-07-23
# last_updated: 2026-07-23
# project: nearform_todo_app
# project_key: NOKEY
# tracking_system: file-system
# story_location: /Users/bmoe/Projects/nearform_todo_app/_bmad-output/implementation-artifacts

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog -> in-progress: Automatically when first story is created (via create-story)
#   - in-progress -> done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# Action Item Status:
#   - open: Committed during a retrospective, not yet addressed
#   - in-progress: Actively being worked on
#   - done: Completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)
# - Retrospective appends its action items to action_items; sprint-status surfaces open ones
#
# EXECUTION SEQUENCE:
# ===================
# Items appear top-to-bottom in dependency-honoring execution order:
#   Epic 1 (Foundation + test/CI harness) -> Epic 2 (backend API) ->
#   Epic 3 (frontend loop, depends on E2) -> Epic 4 (backdrop, after core frontend) ->
#   Epic 5 (containerized delivery, needs backend+frontend) ->
#   Epic 6 (QA/a11y/security/docs sign-off against the composed stack).
# Stories within an epic are ordered by their intra-epic dependencies.
# Note: Story 1.3 wires the coverage gate report-only; Story 6.2 flips it to enforcing.
# Note: The AI-integration log is seeded in 1.1 and appended incrementally through 6.4.

generated: 2026-07-23
last_updated: 2026-07-23
# note: Story 5.2 completed backlog -> ready-for-dev -> in-progress -> review -> done on 2026-07-23 (full BMAD cycle: create-story, dev-story, adversarial code-review). Frontend nginx container + single-origin `docker compose up`: multi-stage node:22 build -> nginx:stable-alpine runtime, non-root nginx (uid 101) on unprivileged port 8080, HEALTHCHECK on GET / (via wget/127.0.0.1 — fixed an IPv6-vs-IPv4 false-unhealthy), nginx.conf serves SPA (try_files fallback) + reverse-proxies /api/* to backend (path-preserving, forwards DELETE-with-body for clear-completed, no CORS per AD-10). Extended 5.1 compose with a `frontend` service depends_on backend healthy, host port 8080. Verified live: 3-service stack builds + healthy in order; SPA served, /api/health proxied 200 db:ok no-CORS, full CRUD + clear-completed DELETE-with-body round-trip through the proxy, SPA fallback; teardown clean. No app code changed; frontend 114 + backend 43 tests pass (44 pre-existing skips). Code-review clean (0 patch/0 decision-needed; 2 defers to 6.3 ops/security, 5 dismissed). Base config stays profile-free (dev/test profiles = 5.3).
# note: Story 5.1 created backlog -> ready-for-dev on 2026-07-23 (create-story). Epic 5 -> in-progress (first story). Backend + Postgres containers: multi-stage non-root backend Dockerfile, migrate-before-serve entrypoint (AD-11), postgres:17 db on named volume pgdata with pg_isready healthcheck, backend depends_on db healthy + Dockerfile healthcheck on GET /api/health. Compose scoped to db+backend only (frontend/single-origin = 5.2, dev/test profiles = 5.3), structured for clean extension.
# note: Story 4.2 completed ready-for-dev -> in-progress -> review -> done on 2026-07-23. Initial dev agent died mid-run (API error) after wiring the error boundary; a finishing agent (same model) added the missing BackdropBoundary test, ran the adversarial code-review in-session (no patch/decision findings; 2 items deferred to Epic 6), and verified green. Epic 4 -> done (both stories done). 114 Vitest tests pass; lint clean; build OK; `three` still isolated in lazy scene-*.js (entry index-*.js has zero three markers); coverage 94.46% stmts / 82.8% branch report-only, degradation.ts 100%.
# note: Story 4.2 created backlog -> ready-for-dev on 2026-07-23 (create-story). Full degradation ladder, watchdog, context-loss recovery, visibility pause, error boundary. Second/last story of Epic 4.
# note: Story 4.1 completed backlog -> ready-for-dev -> in-progress -> review -> done on 2026-07-23 (full BMAD cycle: create-story, dev-story, clean code-review). Epic 4 -> in-progress (first story). Isolated code-split three.js cube-star backdrop; `three` verified out of the entry bundle (separate lazy scene-*.js chunk). 90 Vitest tests, 97.22% stmts/86.79% branch (report-only), lint clean, build OK.
# note: Story 3.5 completed backlog -> ready-for-dev -> in-progress -> review -> done on 2026-07-23 (full BMAD cycle: create-story, dev-story, clean code-review). Epic 3 -> done (all 5 stories done).
# note: Story 3.1 completed ready-for-dev -> in-progress -> review -> done on 2026-07-23 (full BMAD cycle). Epic 3 -> in-progress (first story).
# note: Story 2.1 completed ready-for-dev -> in-progress -> review -> done on 2026-07-23 (full BMAD cycle). Epic 2 -> in-progress (first story).
project: nearform_todo_app
project_key: NOKEY
# note: Story 1.3 completed ready-for-dev -> in-progress -> review -> done on 2026-07-23 (full BMAD cycle). Epic 1 all stories done.
tracking_system: file-system
story_location: /Users/bmoe/Projects/nearform_todo_app/_bmad-output/implementation-artifacts

development_status:
  # --- Epic 1: Project Foundation & Test Harness ---
  epic-1: done
  1-1-repository-skeleton-and-tooling-baseline: done
  1-2-fastapi-app-factory-health-readiness-endpoint-and-db-session-foundation: done
  1-3-github-actions-ci-pipeline: done
  epic-1-retrospective: optional

  # --- Epic 2: Todo CRUD & Clear-Completed API ---
  epic-2: done
  2-1-todo-model-list-and-create-endpoints-with-validation-and-error-envelope: done
  2-2-toggle-and-delete-endpoints: done
  2-3-clear-completed-bulk-endpoint-with-id-snapshot: done
  epic-2-retrospective: optional

  # --- Epic 3: Core Todo Experience (Frontend) ---
  epic-3: done
  3-1-panel-shell-design-tokens-api-client-and-list-view-with-loading-empty-error-states: done
  3-2-add-input-with-optimistic-create-and-client-validation: done
  3-3-todo-row-toggle-completion-in-place-and-delete: done
  3-4-footer-bar-clear-completed-and-deferred-undo-toast: done
  3-5-cross-cutting-keyboard-navigation-screen-reader-semantics-and-responsive-layout: done
  epic-3-retrospective: optional

  # --- Epic 4: Space Backdrop & Graceful Degradation ---
  epic-4: done
  4-1-isolated-three-js-cube-star-backdrop: done
  4-2-mandatory-degradation-performance-guardrails-and-error-boundary: done
  epic-4-retrospective: optional

  # --- Epic 5: Containerized Delivery ---
  epic-5: in-progress
  5-1-backend-database-containers-with-health-checks-named-volume-and-migrate-before-serve: done
  5-2-frontend-container-and-single-origin-docker-compose-up: done
  5-3-dev-and-test-compose-profiles-with-env-var-configuration: backlog
  epic-5-retrospective: optional

  # --- Epic 6: Quality, Accessibility, Security & Documentation Sign-off ---
  epic-6: backlog
  6-1-playwright-e2e-suite-and-automated-accessibility-gate: backlog
  6-2-coverage-gate-at-70-meaningful-coverage: backlog
  6-3-security-review-and-performance-accessibility-pass: backlog
  6-4-readme-and-ai-integration-log: backlog
  epic-6-retrospective: optional
