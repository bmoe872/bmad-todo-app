// Shared wire types mirroring the backend JSON contract (snake_case keys,
// no field-name mapping layer). The canonical `Todo` type and request/response
// shapes are added in Epic 2/3 alongside the API client. Placeholder for Story 1.1.
export {};
