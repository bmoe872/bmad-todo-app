---
topic: Full-stack Todo web app built as a BMAD methodology demo (containerized, tested, with a three.js floating-in-space UI flourish)
updated: 2026-07-23T09:27
---

- (decision) Intent=Create. Purpose is a BMAD methodology demo; the Todo app is the vehicle. Stakes=learning/demo reviewed by trainers, so brief is right-sized not investor-grade.
- (event) User supplied full activity spec (4 steps: specs, build w/ QA-from-day-one, Docker Compose, QA activities) + a supplied PRD. Both preserved in addendum.md as downstream input.
- (decision) Scope: full-stack containerized web app (frontend+backend, optional DB), runs via docker-compose up. Core = CRUD todos (create/view/complete/delete) + empty/loading/error states, responsive desktop+mobile. No auth/multi-user in v1 but architecture must not preclude it.
- (decision) User-added delighter: three.js scene — todo list floating in space with cubes drifting past like stars. Cosmetic flourish, explicitly for fun / to show BMAD handling a non-CRUD feature.
- (decision) Hard success targets from spec: >=70% meaningful coverage, >=5 passing Playwright E2E tests, docker-compose up works, zero critical WCAG AA violations, README + AI-integration log. QA integrated throughout, not bolted on.
- (assumption) Likely stack (to confirm at Architecture): Node.js/Fastify backend, React+Vite frontend, three.js backdrop, volume-backed DB (Postgres) for session durability + better multi-container Compose demo.
- (assumption) three.js delighter needs a prefers-reduced-motion / static fallback to protect the WCAG AA target — flagged for UX/architecture.
- (event) Fast-path full draft of brief.md written from supplied PRD + spec + three.js ask; [ASSUMPTION] tags on stack, DB choice, and reduced-motion fallback.
- (decision) Stack confirmed: backend=Python/FastAPI (user prefers Python), frontend=React+Vite+three.js, DB=PostgreSQL (volume-backed). Removes prior Fastify assumption.
- (decision) prefers-reduced-motion/static fallback for three.js backdrop confirmed as a firm requirement (was assumption).
- (change) Reframe: brief now reads as a genuine product brief for the todo app; BMAD-demo/split-audience framing removed from brief.md. Training/demo context retained in addendum.md only.
- (event) Finalize: doc_standards passes (structure+prose) run as parallel subagents on brief.md (tightened, meaning unchanged) and addendum.md (no edits; source preserved). brief.md status=final. No external handoffs configured.
