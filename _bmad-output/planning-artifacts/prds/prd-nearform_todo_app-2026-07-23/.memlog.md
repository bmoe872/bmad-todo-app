---
topic: nearform_todo_app — minimal polished personal Todo web app
updated: 2026-07-23T09:40
---

- (event) Headless CREATE run. Sources: finalized brief + addendum (Sec A verbatim PRD, B activity spec, C three.js feature).
- (decision) Scope = single-user personal Todo, core loop create/view/complete/delete; no auth/multi-user/collab/priority/deadlines/reminders in v1.
- (decision) Signature feature: three.js floating-in-space backdrop w/ drifting cube stars; MUST have prefers-reduced-motion/static fallback so it never violates WCAG AA or perf budget.
- (decision) Stack fixed by caller: FastAPI backend, React+Vite+three.js frontend, PostgreSQL volume-backed; multi-stage Dockerfiles (non-root, healthchecks) + docker-compose up.
- (decision) Hard quality bars are PRD requirements: >=70% coverage, >=5 Playwright E2E, unit+integration+E2E suites, zero critical WCAG AA violations, docker-compose up, README + AI-integration log; QA from day one.
- (decision) Architecture must not preclude later auth/multi-user but must not build it now.
- (assumption) Todos sorted newest-first by creation time; completion is a toggle (can un-complete).
- (assumption) Editing an existing todo's text is OUT of v1 (supplied PRD lists only create/view/complete/delete).
- (assumption) Instant-feel budget: perceived UI update <100ms (optimistic), API p95 <300ms under normal local conditions.
- (assumption) Single implicit global list (no user scoping); todo text non-empty, max ~500 chars.
- (change) Wrote prd.md (Essential Spine + Cross-Cutting NFRs + Constraints/Guardrails clusters) and PRD addendum (fixed stack, QA tooling, extensibility seam).
- (override) Headless low-stakes training run: skipped interactive Discovery, reviewer-gate subagents, and editorial doc_standards skills; kept status=draft (open questions need human decision).
- (event) PRD draft complete, pending human review.
- (change) Clear-completed moved INTO v1 scope (only structural change). Added FR-9 (bulk-remove all completed in one action, w/ confirm-or-undo consideration deferred to bmad-ux, implies a bulk delete-completed endpoint); added UJ-4; added to Glossary, §6.1 In Scope; removed from §4.1 FR-3 Out-of-Scope and §6.2; reflected in SM-1 and SM-6 (>=5 Playwright E2E now includes a clear-completed journey, validates FR-1..FR-9).
- (decision) Human-confirmed open questions: (1) ordering = newest-first, completed items stay in place (completing only restyles); (2) Description = plain single-line, required/non-empty, max ~500 chars; (3) completed->active toggle = yes. Deferred-with-accepted-defaults to architecture/UX: (5) backdrop perf test devices/viewports, (6) WebGL-unavailable static/plain degradation, (7) numeric latency/frame budgets (~100ms UI, API p95<300ms, ~60fps step-down). PRD status flipped draft->final; working-title 'confirm' caveat removed (name nearform_todo_app accepted). All hard quality bars unchanged.
